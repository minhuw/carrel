<<<<<<<<<<<<<<< conflict 1 of 1
+++++++++++++++ wszmmtyy 10dad267 "carrel: remove chat and agents window smoke tests" (rebase destination)
<<<<<<<<<<< conflict 1 of 1
+++++++++++ wszmmtyy b5218c20 "carrel: remove chat and agents window smoke tests" (rebase destination)
<<<<<<< conflict 1 of 1
%%%%%%% diff from: wszmmtyy 0234ce9a "carrel: remove chat and agents window smoke tests" (parents of rebased revision)
\\\\\\\        to: wszmmtyy 0234ce9a "carrel: remove chat and agents window smoke tests" (rebased revision)
 # Copilot product dependency integration (no-op unless an override is set).
-# Overrides @github/copilot-sdk / @github/copilot to a version published to the
-# private feed and refreshes the lockfile so the node_modules cache key below
-# misses and `npm ci` installs the canary. Authenticates to the feed first so
-# the override's lockfile refresh can resolve the canary — the standard
-# npmAuthenticate runs only after the cache-key step. Mirrors common/agent-sdk-produce.yml.
+# Overrides @github/copilot-sdk with a version published to the private feed,
+# records its bundled runtime version, and refreshes the lockfile so the
+# node_modules cache key below misses and `npm ci` installs the canary.
+# Authenticates before the standard npmAuthenticate step.
 # See microsoft/vscode-engineering specs/sdk-vscode-integration.spec.md.
 
 parameters:
   - name: windows
     type: boolean
     default: false
 
 steps:
   - ${{ if eq(parameters.windows, true) }}:
       - powershell: |
           npm config set registry $env:NPM_REGISTRY
           Write-Host "##vso[task.setvariable variable=SDK_CANARY_NPMRC_PATH]$(npm config get userconfig)"
         condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
         displayName: SDK canary — set registry for auth
   - ${{ else }}:
       - script: |
           set -e
           npm config set registry "$NPM_REGISTRY"
           echo "##vso[task.setvariable variable=SDK_CANARY_NPMRC_PATH]$(npm config get userconfig)"
         condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
         displayName: SDK canary — set registry for auth
 
   - task: npmAuthenticate@0
     inputs:
       workingFile: $(SDK_CANARY_NPMRC_PATH)
     condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
     displayName: SDK canary — authenticate feed
 
   - ${{ if eq(parameters.windows, true) }}:
       - powershell: node build/azure-pipelines/common/apply-sdk-canary-override.ts
         condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
         env:
           VSCODE_SDK_CANARY_VERSION: $(VSCODE_SDK_CANARY_VERSION)
           VSCODE_CLI_CANARY_VERSION: $(VSCODE_CLI_CANARY_VERSION)
         displayName: Apply SDK canary override
   - ${{ else }}:
       - script: node build/azure-pipelines/common/apply-sdk-canary-override.ts
         condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
         env:
           VSCODE_SDK_CANARY_VERSION: $(VSCODE_SDK_CANARY_VERSION)
           VSCODE_CLI_CANARY_VERSION: $(VSCODE_CLI_CANARY_VERSION)
         displayName: Apply SDK canary override
+++++++ slkqvsnl db29c39f "carrel: remove Copilot extension from build plumbing" (rebased revision)
>>>>>>> conflict 1 of 1 ends
%%%%%%%%%%% diff from: wszmmtyy 0234ce9a "carrel: remove chat and agents window smoke tests" (parents of rebased revision)
\\\\\\\\\\\        to: slkqvsnl db29c39f "carrel: remove Copilot extension from build plumbing" (rebased revision)
-# Copilot product dependency integration (no-op unless an override is set).
-# Overrides @github/copilot-sdk / @github/copilot to a version published to the
-# private feed and refreshes the lockfile so the node_modules cache key below
-# misses and `npm ci` installs the canary. Authenticates to the feed first so
-# the override's lockfile refresh can resolve the canary — the standard
-# npmAuthenticate runs only after the cache-key step. Mirrors common/agent-sdk-produce.yml.
-# See microsoft/vscode-engineering specs/sdk-vscode-integration.spec.md.
-
-parameters:
-  - name: windows
-    type: boolean
-    default: false
-
-steps:
-  - ${{ if eq(parameters.windows, true) }}:
-      - powershell: |
-          npm config set registry $env:NPM_REGISTRY
-          Write-Host "##vso[task.setvariable variable=SDK_CANARY_NPMRC_PATH]$(npm config get userconfig)"
-        condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-        displayName: SDK canary — set registry for auth
-  - ${{ else }}:
-      - script: |
-          set -e
-          npm config set registry "$NPM_REGISTRY"
-          echo "##vso[task.setvariable variable=SDK_CANARY_NPMRC_PATH]$(npm config get userconfig)"
-        condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-        displayName: SDK canary — set registry for auth
-
-  - task: npmAuthenticate@0
-    inputs:
-      workingFile: $(SDK_CANARY_NPMRC_PATH)
-    condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-    displayName: SDK canary — authenticate feed
-
-  - ${{ if eq(parameters.windows, true) }}:
-      - powershell: node build/azure-pipelines/common/apply-sdk-canary-override.ts
-        condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-        env:
-          VSCODE_SDK_CANARY_VERSION: $(VSCODE_SDK_CANARY_VERSION)
-          VSCODE_CLI_CANARY_VERSION: $(VSCODE_CLI_CANARY_VERSION)
-        displayName: Apply SDK canary override
-  - ${{ else }}:
-      - script: node build/azure-pipelines/common/apply-sdk-canary-override.ts
-        condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-        env:
-          VSCODE_SDK_CANARY_VERSION: $(VSCODE_SDK_CANARY_VERSION)
-          VSCODE_CLI_CANARY_VERSION: $(VSCODE_CLI_CANARY_VERSION)
-        displayName: Apply SDK canary override
>>>>>>>>>>> conflict 1 of 1 ends
%%%%%%%%%%%%%%% diff from: wszmmtyy 0234ce9a "carrel: remove chat and agents window smoke tests" (parents of rebased revision)
\\\\\\\\\\\\\\\        to: slkqvsnl db29c39f "carrel: remove Copilot extension from build plumbing" (rebased revision)
-# Copilot product dependency integration (no-op unless an override is set).
-# Overrides @github/copilot-sdk / @github/copilot to a version published to the
-# private feed and refreshes the lockfile so the node_modules cache key below
-# misses and `npm ci` installs the canary. Authenticates to the feed first so
-# the override's lockfile refresh can resolve the canary — the standard
-# npmAuthenticate runs only after the cache-key step. Mirrors common/agent-sdk-produce.yml.
-# See microsoft/vscode-engineering specs/sdk-vscode-integration.spec.md.
-
-parameters:
-  - name: windows
-    type: boolean
-    default: false
-
-steps:
-  - ${{ if eq(parameters.windows, true) }}:
-      - powershell: |
-          npm config set registry $env:NPM_REGISTRY
-          Write-Host "##vso[task.setvariable variable=SDK_CANARY_NPMRC_PATH]$(npm config get userconfig)"
-        condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-        displayName: SDK canary — set registry for auth
-  - ${{ else }}:
-      - script: |
-          set -e
-          npm config set registry "$NPM_REGISTRY"
-          echo "##vso[task.setvariable variable=SDK_CANARY_NPMRC_PATH]$(npm config get userconfig)"
-        condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-        displayName: SDK canary — set registry for auth
-
-  - task: npmAuthenticate@0
-    inputs:
-      workingFile: $(SDK_CANARY_NPMRC_PATH)
-    condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-    displayName: SDK canary — authenticate feed
-
-  - ${{ if eq(parameters.windows, true) }}:
-      - powershell: node build/azure-pipelines/common/apply-sdk-canary-override.ts
-        condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-        env:
-          VSCODE_SDK_CANARY_VERSION: $(VSCODE_SDK_CANARY_VERSION)
-          VSCODE_CLI_CANARY_VERSION: $(VSCODE_CLI_CANARY_VERSION)
-        displayName: Apply SDK canary override
-  - ${{ else }}:
-      - script: node build/azure-pipelines/common/apply-sdk-canary-override.ts
-        condition: and(succeeded(), or(ne(variables['VSCODE_SDK_CANARY_VERSION'], ''), ne(variables['VSCODE_CLI_CANARY_VERSION'], '')))
-        env:
-          VSCODE_SDK_CANARY_VERSION: $(VSCODE_SDK_CANARY_VERSION)
-          VSCODE_CLI_CANARY_VERSION: $(VSCODE_CLI_CANARY_VERSION)
-        displayName: Apply SDK canary override
>>>>>>>>>>>>>>> conflict 1 of 1 ends
